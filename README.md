Ai-Generated-Text-Detection

# Setup
1.Open the terminal at the designated folder and run the command:
pip install -r requirements.txt

2.In the test.py , set the dataset file path. You could use the trained models, but i recommend you to generate the models again using the dataset from kaggle. This model is generated on a small sample dataset. 
Dataset link = https://www.kaggle.com/datasets/shanegerami/ai-vs-human-text
